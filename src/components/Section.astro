---
const { class: className, id } = Astro.props;
---

<!-- COMPONENTES DE LAS SECCIONES PARA REUTILIZAR -->
<section id={id} class={`w-full mx-auto lg:w-[740px] ${className}`}>
    <slot />
</section>
